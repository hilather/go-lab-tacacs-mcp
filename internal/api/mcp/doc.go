// Package mcp is the Streamable HTTP adapter for the operation registry.
//
// This skeleton implements server/discover (via the official SDK) and the
// two tools taclab.system.status.get and taclab.policy.evaluate. It invokes
// operations and never the REST adapter.
package mcp
